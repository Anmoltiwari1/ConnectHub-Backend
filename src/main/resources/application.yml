spring:
  application:
    name: presence-service
  datasource:
    url: jdbc:mysql://localhost:3306/connecthub_db?createDatabaseIfNotExist=true
    username: root
    password: Ms7ddhoni123@
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
  data:
    redis:
      host: localhost
      port: 6379

server:
  port: 8085
