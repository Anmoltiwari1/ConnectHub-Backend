spring:
  application:
    name: media-service
  datasource:
    url: jdbc:mysql://localhost:3306/connecthub_db?createDatabaseIfNotExist=true
    username: root
    password: Ms7ddhoni123@
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
  servlet:
    multipart:
      max-file-size: 50MB
      max-request-size: 50MB

server:
  port: 8084

aws:
  s3:
    mock: true
    bucket: connecthub-media
    base-url: http://localhost:8084/uploads
  storage:
    local-path: ./uploads
