spring:
  application:
    name: message-service
  datasource:
    url: jdbc:mysql://localhost:3306/connecthub_db?createDatabaseIfNotExist=true
    username: root
    password: Ms7ddhoni123@
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest

server:
  port: 8083

connecthub:
  rabbitmq:
    exchange: chat.exchange
    queue: chat.queue
    routing-key: chat.routing.key

logging:
  level:
    org.hibernate.SQL: DEBUG
    org.hibernate.orm.jdbc.bind: TRACE
    org.springframework.web: DEBUG
    com.connecthub.message: DEBUG
